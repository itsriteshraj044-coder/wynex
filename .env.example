# Claude API key used by scripts/generate-blog.mjs (the daily AI blog generator).
# Create one at https://console.anthropic.com/  →  copy this file to `.env` and fill it in.
# NEVER commit your real key. In CI, set this as the GitHub Actions secret ANTHROPIC_API_KEY.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
